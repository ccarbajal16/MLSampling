# Create default progress manager

Create default progress manager

## Usage

``` r
create_progress_manager(config_manager = NULL)
```

## Arguments

- config_manager:

  Optional config manager

## Value

ProgressManager instance

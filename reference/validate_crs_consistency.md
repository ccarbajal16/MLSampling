# Validate CRS consistency between spatial objects

Validate CRS consistency between spatial objects

## Usage

``` r
validate_crs_consistency(boundary, covariates)
```

## Arguments

- boundary:

  sf boundary object

- covariates:

  SpatRaster covariates object

## Value

List with CRS validation results

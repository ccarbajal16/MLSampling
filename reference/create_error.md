# Create a custom error condition

Create a custom error condition

## Usage

``` r
create_error(message, class, call = sys.call(-1), ...)
```

## Arguments

- message:

  Error message

- class:

  Error class name

- call:

  Call expression

## Value

error condition object

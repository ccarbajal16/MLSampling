# Validate feature importance scores

Validate feature importance scores

## Usage

``` r
validate_feature_importance(feature_importance, covariates)
```

## Arguments

- feature_importance:

  Numeric vector of feature importance scores

- covariates:

  SpatRaster with covariate layers

## Value

List with validation results

# Create default resource manager

Create default resource manager

## Usage

``` r
create_resource_manager(config_manager = NULL)
```

## Arguments

- config_manager:

  Optional config manager

## Value

ResourceManager instance

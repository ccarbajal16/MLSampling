# Validate boundary geometry

Validate boundary geometry

## Usage

``` r
validate_field_boundary_geometry(boundary, strict_validation = TRUE)
```

## Arguments

- boundary:

  sf object with field boundary

- strict_validation:

  Logical for strict validation mode

## Value

List with validation results

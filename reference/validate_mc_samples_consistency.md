# Validate Monte Carlo samples consistency

Validate Monte Carlo samples consistency

## Usage

``` r
validate_mc_samples_consistency(mc_samples, n_samples)
```

## Arguments

- mc_samples:

  Monte Carlo samples

- n_samples:

  Number of samples

## Value

List with validation results
